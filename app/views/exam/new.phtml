<?php
/** @var string $title */
/** @var App\Models\Exam $exam */
/** @var array<int, array{patient: App\Models\Patient, user: App\Models\User|null}> $patientsWithUser */
/** @var array<int, App\Models\ExamType> $examTypes */
?>

<div class="container py-4">

    <div class="mb-4">
        <h1 class="h3 fw-bold text-primary mb-1">
            <i class="bi bi-file-earmark-medical me-2"></i>
            <?= htmlspecialchars($title) ?>
        </h1>

        <p class="text-muted mb-0">
            Anexe um novo exame de um paciente
        </p>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body">

            <form action="<?= route('exams.create') ?>" method="POST" enctype="multipart/form-data">

                <div class="row g-3">

                    <div class="col-md-6">
                        <label class="form-label fw-semibold">
                            Paciente
                        </label>

                        <select name="patient_id" class="form-select <?= $exam->errors('patient_id') ? 'is-invalid' : '' ?>">
                            <option value="">Selecione um paciente</option>

                            <?php foreach ($patientsWithUser as $item): ?>
                                <?php
                                $patient = $item['patient'];
                                $patientUser = $item['user'] ?? null;
                                ?>

                                <option
                                    value="<?= $patient->id ?>"
                                    <?= $exam->patient_id == $patient->id ? 'selected' : '' ?>
                                >
                                    <?= htmlspecialchars($patientUser ? $patientUser->name : 'Paciente sem usuário') ?>
                                </option>
                            <?php endforeach; ?>
                        </select>

                        <?php if ($exam->errors('patient_id')): ?>
                            <div class="invalid-feedback"><?= $exam->errors('patient_id') ?></div>
                        <?php endif; ?>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-semibold">
                            Tipo de exame
                        </label>

                        <select name="exam_type_id" class="form-select <?= $exam->errors('exam_type_id') ? 'is-invalid' : '' ?>">
                            <option value="">Selecione um tipo</option>

                            <?php foreach ($examTypes as $examType): ?>
                                <option
                                    value="<?= $examType->id ?>"
                                    <?= $exam->exam_type_id == $examType->id ? 'selected' : '' ?>
                                >
                                    <?= htmlspecialchars($examType->name) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>

                        <?php if ($exam->errors('exam_type_id')): ?>
                            <div class="invalid-feedback"><?= $exam->errors('exam_type_id') ?></div>
                        <?php endif; ?>
                    </div>

                    <div class="col-12">
                        <label class="form-label fw-semibold">
                            Arquivo do exame (PDF)
                        </label>

                        <input
                            type="file"
                            name="exam_file"
                            class="form-control"
                            accept="application/pdf"
                            required
                        >
                    </div>

                </div>

                <hr class="my-4">

                <div class="d-flex justify-content-between">
                    <a href="<?= route('exams.index') ?>" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i>
                        Voltar
                    </a>

                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-circle me-1"></i>
                        Anexar exame
                    </button>
                </div>

            </form>

        </div>
    </div>

</div>
