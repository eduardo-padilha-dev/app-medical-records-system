<?php
/** @var string $title */
/** @var App\Models\Appointment $appointment */
/** @var array $patients */
/** @var array $doctors */
?>

<div class="container py-4">

    <div class="mb-4">
        <h1 class="h3 fw-bold text-primary mb-1">
            <i class="bi bi-pencil-square me-2"></i>
            <?= $title ?>
        </h1>

        <p class="text-muted mb-0">
            Atualize os dados do agendamento selecionado.
        </p>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body">

            <form action="<?= route('appointments.update', ['id' => $appointment->id]) ?>" method="POST">
                <input type="hidden" name="_method" value="PUT">

                <div class="row g-3">

                    <div class="col-md-6">
                        <label class="form-label fw-semibold">
                            Paciente
                        </label>

                        <select name="patient_id" class="form-select <?= $appointment->errors('patient_id') ? 'is-invalid' : '' ?>">
                            <?php foreach ($patients as $patient): ?>
                                <?php $user = $patient->user(); ?>

                                <option
                                    value="<?= $patient->id ?>"
                                    <?= $appointment->patient_id == $patient->id ? 'selected' : '' ?>
                                >
                                    <?= $user ? $user->name : 'Paciente sem usuário' ?>
                                </option>
                            <?php endforeach; ?>
                        </select>

                        <?php if ($appointment->errors('patient_id')): ?>
                            <div class="invalid-feedback"><?= $appointment->errors('patient_id') ?></div>
                        <?php endif; ?>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-semibold">
                            Médico
                        </label>

                        <select name="doctor_id" class="form-select <?= $appointment->errors('doctor_id') ? 'is-invalid' : '' ?>">
                            <?php foreach ($doctors as $doctor): ?>
                                <?php $user = $doctor->user(); ?>

                                <option
                                    value="<?= $doctor->id ?>"
                                    <?= $appointment->doctor_id == $doctor->id ? 'selected' : '' ?>
                                >
                                    <?= $user ? $user->name : 'Médico sem usuário' ?>
                                </option>
                            <?php endforeach; ?>
                        </select>

                        <?php if ($appointment->errors('doctor_id')): ?>
                            <div class="invalid-feedback"><?= $appointment->errors('doctor_id') ?></div>
                        <?php endif; ?>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-semibold">
                            Data e hora
                        </label>

                        <input
                            type="datetime-local"
                            name="scheduled_at"
                            class="form-control <?= $appointment->errors('scheduled_at') ? 'is-invalid' : '' ?>"
                            value="<?= date('Y-m-d\TH:i', strtotime($appointment->scheduled_at)) ?>"
                        >

                        <?php if ($appointment->errors('scheduled_at')): ?>
                            <div class="invalid-feedback"><?= $appointment->errors('scheduled_at') ?></div>
                        <?php endif; ?>
                    </div>

                    <div class="col-md-6">
                        <label class="form-label fw-semibold">
                            Status
                        </label>

                        <select name="status" class="form-select <?= $appointment->errors('status') ? 'is-invalid' : '' ?>">
                            <option value="scheduled" <?= $appointment->status === 'scheduled' ? 'selected' : '' ?>>
                                Agendado
                            </option>

                            <option value="confirmed" <?= $appointment->status === 'confirmed' ? 'selected' : '' ?>>
                                Confirmado
                            </option>

                            <option value="completed" <?= $appointment->status === 'completed' ? 'selected' : '' ?>>
                                Concluído
                            </option>

                            <option value="canceled" <?= $appointment->status === 'canceled' ? 'selected' : '' ?>>
                                Cancelado
                            </option>
                        </select>

                        <?php if ($appointment->errors('status')): ?>
                            <div class="invalid-feedback"><?= $appointment->errors('status') ?></div>
                        <?php endif; ?>
                    </div>


                    <div class="col-12">
                      <label class="form-label fw-semibold">
                          Observação
                      </label>

                      <textarea
                          name="observation"
                          class="form-control"
                          rows="3"
                          placeholder="Digite uma observação sobre o agendamento"
                      ><?= htmlspecialchars($appointment->observation ?? '') ?></textarea>
                  </div>
                </div>

                <hr class="my-4">

                <div class="d-flex justify-content-between">
                    <a href="<?= route('appointments.show', ['id' => $appointment->id]) ?>" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i>
                        Voltar
                    </a>

                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-circle me-1"></i>
                        Atualizar Agendamento
                    </button>
                </div>

            </form>

        </div>
    </div>

</div>
