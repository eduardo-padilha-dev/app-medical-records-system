<?php
/** @var string $title */
/** @var App\Models\MedicalRecord $medicalRecord */
/** @var App\Models\Patient|null $patient */
/** @var App\Models\Doctor|null $doctor */

$currentUser    = $this->currentUser();
$patientUser = $patient ? $patient->user() : null;
$doctorUser = $doctor ? $doctor->user() : null;
?>

<div class="container py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <div>
            <h1 class="h3 fw-bold text-primary mb-1">
                <i class="bi bi-file-medical me-2"></i>
                <?= htmlspecialchars($title) ?>
            </h1>

            <p class="text-muted mb-0">
                Visualização completa do prontuário médico.
            </p>
        </div>

        <span class="badge text-bg-primary fs-6">
            Registro #<?= $medicalRecord->id ?>
        </span>

    </div>

    <div class="card border-0 shadow-sm">

        <div class="card-body">

            <div class="row g-4">

                <div class="col-md-6">
                    <div class="p-3 border rounded bg-light h-100">

                        <small class="text-muted d-block mb-1">
                            Paciente
                        </small>

                        <strong>
                            <?= htmlspecialchars(
                                $patientUser
                                    ? $patientUser->name
                                    : 'Paciente desconhecido'
                            ) ?>
                        </strong>

                    </div>
                </div>

                <div class="col-md-6">
                    <div class="p-3 border rounded bg-light h-100">

                        <small class="text-muted d-block mb-1">
                            Médico
                        </small>

                        <strong>
                            <?= htmlspecialchars(
                                $doctorUser
                                    ? $doctorUser->name
                                    : 'Médico desconhecido'
                            ) ?>
                        </strong>

                    </div>
                </div>

                <div class="col-md-6">
                    <div class="p-3 border rounded bg-light h-100">

                        <small class="text-muted d-block mb-1">
                            Data do registro
                        </small>

                        <strong>
                            <?= date(
                                'd/m/Y',
                                strtotime($medicalRecord->record_date)
                            ) ?>
                        </strong>

                    </div>
                </div>

                <?php if ($medicalRecord->appointment_id): ?>

                    <div class="col-md-6">
                        <div class="p-3 border rounded bg-light h-100">

                            <small class="text-muted d-block mb-1">
                                Agendamento vinculado
                            </small>

                            <strong>
                                #<?= $medicalRecord->appointment_id ?>
                            </strong>

                        </div>
                    </div>

                <?php endif; ?>

                <div class="col-12">
                    <div class="p-3 border rounded">

                        <small class="text-muted d-block mb-2">
                            Diagnóstico
                        </small>

                        <p class="mb-0">
                            <?= nl2br(htmlspecialchars($medicalRecord->diagnosis)) ?>
                        </p>

                    </div>
                </div>

                <div class="col-12">
                    <div class="p-3 border rounded">

                        <small class="text-muted d-block mb-2">
                            Prescrição
                        </small>

                        <p class="mb-0">
                            <?= nl2br(htmlspecialchars($medicalRecord->prescription ?: 'Nenhuma prescrição informada.')) ?>
                        </p>

                    </div>
                </div>

                <div class="col-12">
                    <div class="p-3 border rounded">

                        <small class="text-muted d-block mb-2">
                            Observações
                        </small>

                        <p class="mb-0">
                            <?= nl2br(htmlspecialchars($medicalRecord->notes ?: 'Nenhuma observação registrada.')) ?>
                        </p>

                    </div>
                </div>

            </div>

            <hr class="my-4">

            <div class="d-flex justify-content-between">

                <a
                    href="<?= route('medical_records.index') ?>"
                    class="btn btn-outline-secondary"
                >
                    <i class="bi bi-arrow-left me-1"></i>
                    Voltar
                </a>

                <?php if ($currentUser->isDoctor()): ?>
                    <a
                        href="<?= route('medical_records.edit', ['id' => $medicalRecord->id]) ?>"
                        class="btn btn-primary"
                    >
                        <i class="bi bi-pencil me-1"></i>
                        Editar Prontuário
                    </a>
                <?php endif; ?>

            </div>

        </div>

    </div>

</div>
