<?php

/**
 * View: medical_record/show.phtml
 * Variáveis recebidas:
 *   $medicalRecord — MedicalRecord
 *   $patient       — Patient
 *   $doctor        — Doctor
 */
/**
 * @var App\Models\MedicalRecord $medicalRecord
 * @var App\Models\Patient       $patient
 * @var App\Models\Doctor        $doctor
 */

use App\Models\User;

$currentUser = $this->currentUser();
$patientUser = $patient ? User::findById($patient->user_id) : null;
$doctorUser  = $doctor  ? User::findById($doctor->user_id)  : null;

// Formata a data para exibição
$recordDate = \DateTime::createFromFormat('Y-m-d', $medicalRecord->record_date);
$dateFormatted = $recordDate ? $recordDate->format('d/m/Y') : $medicalRecord->record_date;
?>

<div class="container py-4">

    <!-- Cabeçalho com breadcrumb -->
    <nav aria-label="breadcrumb" class="mb-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="<?= route('medical_records.index') ?>">Prontuários</a>
            </li>
            <li class="breadcrumb-item active">Prontuário #<?= $medicalRecord->id ?></li>
        </ol>
    </nav>

    <div class="d-flex justify-content-between align-items-start mb-4">
        <h1 class="h3 fw-bold text-primary">
            <i class="bi bi-file-earmark-medical me-2"></i>Prontuário #<?= $medicalRecord->id ?>
        </h1>

        <!-- Ações disponíveis apenas para o médico que criou -->
        <?php if ($currentUser->isDoctor()): ?>
            <div class="d-flex gap-2">
                <a href="<?= route('medical_records.edit', ['id' => $medicalRecord->id]) ?>"
                    class="btn btn-outline-warning">
                    <i class="bi bi-pencil me-1"></i>Editar
                </a>

                <button class="btn btn-outline-danger"
                    data-bs-toggle="modal" data-bs-target="#modalDelete">
                    <i class="bi bi-trash me-1"></i>Excluir
                </button>
            </div>
        <?php endif; ?>
    </div>

    <div class="row g-4">

        <!-- Coluna principal: dados clínicos -->
        <div class="col-lg-8">

            <!-- Card: Diagnóstico (campo mais importante) -->
            <div class="card border-0 shadow-sm mb-4">
                <div class="card-header bg-primary text-white">
                    <i class="bi bi-clipboard2-pulse me-2"></i>
                    <strong>Diagnóstico</strong>
                </div>
                <div class="card-body">
                    <p class="mb-0" style="white-space: pre-wrap;"><?= htmlspecialchars($medicalRecord->diagnosis) ?></p>
                </div>
            </div>

            <!-- Card: Prescrição -->
            <div class="card border-0 shadow-sm mb-4">
                <div class="card-header bg-light">
                    <i class="bi bi-capsule me-2 text-success"></i>
                    <strong>Prescrição Médica</strong>
                </div>
                <div class="card-body">
                    <?php if ($medicalRecord->prescription): ?>
                        <p class="mb-0" style="white-space: pre-wrap;"><?= htmlspecialchars($medicalRecord->prescription) ?></p>
                    <?php else: ?>
                        <span class="text-muted fst-italic">Nenhuma prescrição registrada.</span>
                    <?php endif; ?>
                </div>
            </div>

            <!-- Card: Observações -->
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-light">
                    <i class="bi bi-journal-text me-2 text-info"></i>
                    <strong>Observações</strong>
                </div>
                <div class="card-body">
                    <?php if ($medicalRecord->notes): ?>
                        <p class="mb-0" style="white-space: pre-wrap;"><?= htmlspecialchars($medicalRecord->notes) ?></p>
                    <?php else: ?>
                        <span class="text-muted fst-italic">Nenhuma observação registrada.</span>
                    <?php endif; ?>
                </div>
            </div>

        </div>

        <!-- Coluna lateral: metadados do prontuário -->
        <div class="col-lg-4">

            <!-- Card: Informações gerais -->
            <div class="card border-0 shadow-sm mb-4">
                <div class="card-header bg-light">
                    <i class="bi bi-info-circle me-2"></i>
                    <strong>Informações</strong>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item d-flex justify-content-between align-items-start">
                        <span class="text-muted">Data</span>
                        <span class="fw-semibold"><?= $dateFormatted ?></span>
                    </li>
                    <li class="list-group-item d-flex justify-content-between align-items-start">
                        <span class="text-muted">ID</span>
                        <span class="badge bg-secondary">#<?= $medicalRecord->id ?></span>
                    </li>
                    <?php if ($medicalRecord->appointment_id): ?>
                        <li class="list-group-item d-flex justify-content-between align-items-start">
                            <span class="text-muted">Consulta vinculada</span>
                            <span>#<?= $medicalRecord->appointment_id ?></span>
                        </li>
                    <?php endif; ?>
                </ul>
            </div>

            <!-- Card: Paciente -->
            <div class="card border-0 shadow-sm mb-4">
                <div class="card-header bg-light">
                    <i class="bi bi-person me-2 text-primary"></i>
                    <strong>Paciente</strong>
                </div>
                <div class="card-body">
                    <?php if ($patientUser): ?>
                        <p class="mb-1 fw-semibold"><?= htmlspecialchars($patientUser->name) ?></p>
                        <p class="text-muted small mb-1"><?= htmlspecialchars($patientUser->email) ?></p>
                        <?php if ($patient->birth_date): ?>
                            <p class="text-muted small mb-0">
                                <i class="bi bi-calendar3 me-1"></i>
                                <?php
                                $bd = \DateTime::createFromFormat('Y-m-d', $patient->birth_date);
                                echo $bd ? $bd->format('d/m/Y') : $patient->birth_date;
                                ?>
                            </p>
                        <?php endif; ?>
                    <?php else: ?>
                        <span class="text-muted">Paciente não encontrado.</span>
                    <?php endif; ?>
                </div>
            </div>

            <!-- Card: Médico responsável -->
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-light">
                    <i class="bi bi-person-badge me-2 text-success"></i>
                    <strong>Médico Responsável</strong>
                </div>
                <div class="card-body">
                    <?php if ($doctorUser): ?>
                        <p class="mb-1 fw-semibold"><?= htmlspecialchars($doctorUser->name) ?></p>
                        <p class="text-muted small mb-1"><?= htmlspecialchars($doctor->specialty) ?></p>
                        <p class="text-muted small mb-0">
                            CRM: <?= htmlspecialchars($doctor->license_number) ?>
                        </p>
                    <?php else: ?>
                        <span class="text-muted">Médico não encontrado.</span>
                    <?php endif; ?>
                </div>
            </div>

        </div>
    </div>

    <!-- Link de retorno -->
    <div class="mt-4">
        <a href="<?= route('medical_records.index') ?>" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1"></i>Voltar à lista
        </a>
    </div>

</div>

<!-- Modal de confirmação de exclusão (só renderiza para médicos) -->
<?php if ($currentUser->isDoctor()): ?>
    <div class="modal fade" id="modalDelete" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header border-0">
                    <h5 class="modal-title text-danger">
                        <i class="bi bi-exclamation-triangle me-2"></i>Confirmar Exclusão
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    Tem certeza que deseja excluir o <strong>Prontuário #<?= $medicalRecord->id ?></strong>?
                    <br><span class="text-danger small">Esta ação não pode ser desfeita.</span>
                </div>
                <div class="modal-footer border-0">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <form method="POST" action="/medical_records/<?= $medicalRecord->id ?>">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash me-1"></i>Excluir
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
<?php endif; ?>