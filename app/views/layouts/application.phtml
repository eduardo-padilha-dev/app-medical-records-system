<?php
/** @var string $title */
/** @var string $view */

$currentUser = $this->currentUser();
?>

<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($title ?? 'Medical Records System') ?> - Medical Records System</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/assets/css/application.css">
    <link rel="stylesheet" href="/assets/css/login.css">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" defer></script>
    <script src="/assets/js/application.js" defer></script>
</head>

<body class="bg-light">

    <?php if ($currentUser): ?>
        <nav class="navbar navbar-expand-lg bg-white border-bottom shadow-sm">
            <div class="container-fluid px-4">

                <a class="navbar-brand fw-bold text-primary" href="<?= route('auth.check') ?>">
                    <i class="bi bi-hospital me-2"></i>
                    Medical Records
                </a>

                <div class="d-flex align-items-center gap-3">
                    <span class="text-muted small">
                        <?= htmlspecialchars($currentUser->name ?? 'Usuário') ?>
                    </span>

                    <form method="POST" action="<?= route('users.logout') ?>" class="mb-0">
                        <input type="hidden" name="_method" value="DELETE">

                        <button type="submit" class="btn btn-outline-danger btn-sm">
                            <i class="bi bi-box-arrow-right me-1"></i>
                            Sair
                        </button>
                    </form>
                </div>

            </div>
        </nav>
    <?php endif; ?>

    <main class="container-fluid py-3">

        <?php require '_flash_message.phtml'; ?>

        <?php require $view; ?>

    </main>

</body>

</html>
